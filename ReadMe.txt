

Question 1.) FirstRepeatingChar.java :  This program returns the first repeating character, i.e the one which occurs first in the string and not the one which gets repeated first.

	Example : Input : abcdbcdba
		  Output: a


Question 2.) StringReverseIterative.java : This program outputs the reverse of a string, there can be multiple methods to do so.
	                                   This one is iterative version.
